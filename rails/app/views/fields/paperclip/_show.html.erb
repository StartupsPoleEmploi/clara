<% if field.url_only? -%>
  <%= link_to field.url, field.url if field.url.present? %>
<% else -%>
  <%= image_tag(field.url, "data-paperclip" => "yes", "alt" => "image bientôt disponible") if field.url.present? %>
<% end -%>

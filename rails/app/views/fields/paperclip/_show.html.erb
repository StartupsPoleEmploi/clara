<% if field.url_only? -%>
  <%= link_to field.url, field.url if field.url.present? %>
<% else -%>
  <%= image_tag(field.url, "data-paperclip" => "yes", "alt" => "l'image n'a pas pu être affichée.") if field.url.present? %>
<% end -%>

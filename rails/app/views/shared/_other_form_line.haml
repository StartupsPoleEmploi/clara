- id ||= ""
- current_val ||= ""
- current_text ||= ""

.tooltip-responsive-swap--bottom
  .c-radioline.js-enterspace{id: "radio_#{id}"}
    .c-radiocontainer
      = check_box_tag "other_form[#{id}]", |
                      "#{id}", |
                      "#{current_val}" == "oui", | 
                      id: "#{id}", |
                      class: "c-radioinput"
    %label.c-radiolabel{for: "#{id}"}
      =current_text
    
.c-filterbox
  %input.checkbox{:id => "lists[new_#{id}]", :name => "lists[new_#{id}]", :type => "checkbox"}/
  %label{:for => "lists[new_#{id}]"}
    %span.check ✓
    Travel without jump

<%#
# Flash Partial

This partial renders flash messages on every page.

## Relevant Helpers:

- `flash`:
  Returns a hash,
  where the keys are the type of flash (alert, error, notice, etc)
  and the values are the message to be displayed.
%>

<% if flash.any? %>
  <div class="flashes hidden">

  </div>
  <button class="js-tooltip hidden"
data-tooltip-content-id="tooltip_flash">
  Show me another dialog tooltip window
</button>
<div id="tooltip_flash" class="hidden">
    <% flash.each do |key, value| -%>
      <div class="flash flash-<%= key %>"><%= value.html_safe %></div>
    <% end -%>
</div>
<% end %>

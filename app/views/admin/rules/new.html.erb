<%- title_simple = t("helpers.creation.rule.new_simple_resource", name: display_resource_name(page.resource_name).titleize) %>
<%- title_composite = t("helpers.creation.rule.new_composite_resource", name: display_resource_name(page.resource_name).titleize) %>
<%- kind = page.resource.try(:kind) || params[:rule_kind] %>
<%- actual_title = kind == "composite" ? title_composite : title_simple %>

<% content_for(:title) do %>
  <%= actual_title %>
<% end %>

<header class="main-content__header" role="banner">
  <h1 class="main-content__page-title">
    <%= content_for(:title) %>
  </h1>

  <div>
    <%= link_to t("administrate.actions.back"), admin_rules_path, class: "button" %>
  </div>
</header>

<section class="main-content__body">
  <%= render "form", local_assigns %>
</section>

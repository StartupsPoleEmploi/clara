require "test_helper"

class SerializeResultsServiceApiEligibleTest < ActiveSupport::TestCase
  
  test '_.api_eligible' do
    #given
    #when
    res = SerializeResultsService.api_eligible(asker, filters, need_filters, custom_filters, custom_parent_filters)
    #then
  end

end
